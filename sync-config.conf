# Sync configuration file
# Source this file or pass it with -c/--config flag

# Required settings
SOURCE_DIR="."
REMOTE_USER="rainer"
REMOTE_HOST="dev-01"
REMOTE_PATH="/home/rainer/live/2026-03-02-basta"

# Optional settings
#SSH_PORT=22
#SSH_KEY="~/.ssh/id_rsa"
#DRY_RUN=false

# Additional exclusion patterns (beyond defaults)
# The script already excludes: node_modules/, .git/, .DS_Store, etc.
EXCLUDES=(
    "sync-config.conf"
    "sync-to-server.sh"
    ".syncignore"
)